import fns from 'mtg-tools/utils/fns';
import { module, test } from 'qunit';

module('Unit | Utility | fns', function(hooks) {

  // Replace this with your real tests.
  test('it works', function(assert) {
    let result = fns();
    assert.ok(result);
  });
});
