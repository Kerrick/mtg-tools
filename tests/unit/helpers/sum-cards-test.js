
import { sumCards } from 'mtg-tools/helpers/sum-cards';
import { module, test } from 'qunit';

module('Unit | Helper | sum cards');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = sumCards([42]);
  assert.ok(result);
});

